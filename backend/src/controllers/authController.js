/**
 * Authentication is handled exclusively by Supabase Auth.
 * Custom bcrypt/JWT authentication controller methods (registerUser, loginUser, logoutUser)
 * and password_hash column references have been removed as they are redundant with Supabase Auth.
 */

module.exports = {};
